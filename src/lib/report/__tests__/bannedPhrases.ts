/**
 * 사용자에게 보이는 문장에 절대 나오면 안 되는 표현.
 * `docs/00-product-brief.md` 의 윤리 가드레일과 `.claude/skills/report-voice` 가 기준이다.
 *
 * 카피 테스트가 여러 파일로 늘면서 이 목록이 복사되기 시작했다. 한 곳에 둔다.
 */
export const BANNED_PHRASES = [
  /부적합/,
  /협업\s*불가/,
  /맞지\s*않는\s*사람/,
  /자질이\s*없/,
  /질병/,
  /수명/,
  /건강/,
  // 미래 단정
  /할\s*것이다/,
  /하게\s*된다/,
  // 건강이나 번아웃을 연상시키는 단어. 비유로도 안 쓴다
  /체온/,
  /피로/,
  /소진/,
]

/**
 * 처방이 실제 행동을 담고 있나.
 * "소통을 잘하세요" 같은 문장은 처방이 아니라 훈계다.
 */
export const VAGUE_PRESCRIPTION = [
  /^소통을/,
  /^협업을/,
  /^서로를/,
  /^노력하/,
  /^잘\s/,
]
