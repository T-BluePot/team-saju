# 화면 캡쳐

`.claude/agents/product-critic.md` 가 이 폴더를 본다. **화면을 크게 바꾸면 여기도 같이 갈아끼운다.**
옛 캡쳐가 남아 있으면 지금 없는 화면을 근거로 지적이 나온다.

| | |
|---|---|
| `*.png` | 지금 화면. 번호는 사용자가 지나는 순서 |
| `compare/*.jpg` | 개편 전후 비교본. 위가 기존, 아래가 현재 |

마지막 갱신은 #59 디자인 개편(PR #60) 시점이다. 375px 기준이고 `16~18` 은 어둡게,
`19` 는 320px 이다.
