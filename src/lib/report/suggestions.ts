import type { Element } from '../saju/types'

/**
 * 해보면 좋은 것.
 *
 * 결과가 일하는 방식까지만 말하고, 그래서 뭘 하면 되는지는 팀 처방에만 있었다.
 * 개인 쪽에도 한 칸을 둔다.
 *
 * **무엇에 매핑했나.** 일간 오행과 강약과 부족 오행 셋 중에 **부족 오행**을 골랐다.
 * 이 칸의 이름이 "해보면 좋은 것" 이라 채우는 쪽이 말이 맞고, 팀 결과의 기운 카드가
 * 이미 같은 논리로 돌아가고 있어서 두 화면이 같은 방식으로 읽힌다.
 *
 * 강약까지 곱하면 열다섯 묶음이 되는데 그만큼 다르게 쓸 말이 없다. 억지로 채우면
 * 표에 없는 얘기를 지어내게 된다. 다섯 묶음으로 둔다.
 *
 * **건강 조언으로 읽히면 안 된다.** `docs/00-product-brief.md` 의 금지선에 제일 가까운
 * 자리다. 그래서 몸에 관한 것은 아예 안 넣는다. 운동도 식단도 없다. 여기 있는 건
 * 주말에 해볼 만한 거리이고, 화면에도 재미로 골라본 것이라고 적는다.
 *
 * 하지 말라는 말은 없다. 다섯 다 해보라는 쪽으로만 쓴다.
 */
export const SUGGESTIONS: Record<Element, [string, string, string]> = {
  // 새로 벌이고 뻗어 나가는 기운
  木: ['안 해본 취미 하나 시작해보기', '방에 화분 하나 들이기', '안 가본 동네 한 바퀴 돌아보기'],
  // 드러내고 사람들 사이에 서는 기운
  火: ['좋아하는 걸 남한테 소개해보기', '사람 많은 자리에 한 번 나가보기', '노래방 가기'],
  // 쌓고 갈무리하는 기운
  土: ['재료부터 사서 한 끼 차려보기', '서랍 한 칸만 정리하기', '오래 걸리는 만들기 붙잡아보기'],
  // 자르고 끝맺는 기운
  金: ['안 쓰는 물건 열 개 비우기', '찍어둔 사진에서 열 장만 고르기', '규칙 있는 보드게임 한판'],
  // 흐르고 되묻는 기운
  水: ['물가 근처로 나들이 가기', '안 읽던 분야 책 한 권', '자기 전에 세 줄 적어두기'],
}

export function suggestionsFor(lacking: Element): [string, string, string] {
  return SUGGESTIONS[lacking]
}
