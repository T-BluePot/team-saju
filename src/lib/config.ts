/**
 * 배포와 외부 연동에 쓰는 값.
 *
 * 주소를 여기저기 박아두면 도메인을 바꿀 때 놓치는 자리가 생긴다.
 * 특히 공유 카드는 한 번 퍼지면 못 고치는 이미지라 낡은 주소가 남는다.
 */

/**
 * 배포 주소.
 *
 * 공유 카드 푸터와 카카오 공유 링크가 같은 값을 쓴다. 끝에 슬래시를 안 붙인다.
 * 카드에 그릴 때 `https://` 를 떼고 보여줘서, 붙어 있으면 빈 줄처럼 보인다.
 *
 * 커스텀 도메인을 붙이면 여기만 고친다. 다만 그 시점 이전에 퍼진 카드에는
 * 옛 주소가 그대로 남으니 도메인은 한 번에 정하는 게 낫다.
 */
export const SITE_URL = 'https://team-saju.bluehotpot187.workers.dev'

/** 카드에 그리고 링크에도 쓰는 사람이 읽는 형태 */
export const SITE_HOST = SITE_URL.replace(/^https?:\/\//, '')

/**
 * 카카오 JavaScript 키.
 *
 * **클라이언트 번들에 그대로 박히는 값이다.** 그래서 카카오 개발자 콘솔에서
 * 플랫폼 도메인을 배포 주소로 제한해 둬야 한다. 제한이 없으면 아무 사이트나
 * 이 키로 공유를 보낼 수 있다.
 *
 * 비어 있으면 카카오 버튼을 안 그린다. 키 없이 눌렀을 때 조용히 아무 일도
 * 안 일어나는 것보다 버튼이 없는 편이 낫다.
 */
export const KAKAO_JS_KEY = String(import.meta.env.VITE_KAKAO_JS_KEY ?? '')
