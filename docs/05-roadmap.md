# 로드맵

## Sprint 0 기획 (완료)

- [x] 프로덕트 브리프, PRD
- [x] 사주 계산 명세 (경계 케이스 라이브러리 검증 포함)
- [x] 아키텍처, 기술 스택
- [x] AI 에이전트 설계

## Sprint 1 MVP (진행중)

- [x] 스캐폴딩 (Vite + React 19 + TS + Tailwind v4)
- [x] 사주 엔진 (시간 보정, 8글자, 오행, 강약, 십신)
- [x] 골든 테스트 8종
- [x] 팀 분석 (오행 합산, 페어 케미, 균형 점수)
- [x] 팀 유형 판정 + 21유형 카피 작성
- [x] 개인정보 동의 모달
- [x] 팀원별 대리 입력 확인 라디오 + 동의 출처 배지
- [x] 상시 고지 문구
- [x] 입력 UI (팀원 CRUD, 저장 없음)
- [x] 결과 UI (개인 카드, 오행 지도, 페어 매트릭스)
- [x] 룰 기반 팀 리포트 (유형, 강점, 처방, 필요한 사람)
- [x] 공유 뷰 분리 + 이미지 카드 추출 (canvas)
- [x] 반응형, 다크모드
- [x] `.claude/` 에이전트 환경

## Sprint 2 AI 리포트

- [ ] `/api/interpret` 서버리스 함수 + dev 미들웨어
- [ ] 4단계 파이프라인 (Persona, Synergy, Playbook, Editor)
- [ ] 가드레일 필터, 폴백
- [ ] SSE 스트리밍 UI
- [ ] prompt caching, 레이트리밋

## Sprint 3 공유

- [ ] 링크 공유. SharePayload 인코딩, 팀 집계만
- [ ] 마크다운 복사
- [ ] 유입 CTA 동선
- [ ] 용어 온보딩 툴팁

## Backlog

- [ ] 대운, 세운. "지금 이 시기의 팀"
- [ ] 합충형해 (지지 상호작용)
- [ ] 용신 추정으로 "팀에 필요한 사람" 제안
- [ ] 초대 링크로 본인이 직접 입력. 서버 저장, 보유기간 30일, 개인정보처리방침 정식 작성
- [ ] 다국어 (영, 일)

## 리스크

| 리스크 | 대응 |
|---|---|
| 절기 경계 계산 오류 | 라이브러리 위임 + 골든셋 G2/G3 고정 |
| 유파별 해석 차이로 컴플레인 | 계산 근거 노출, "간이 판정"임을 명시 |
| AI가 단정적으로 씀 | 프롬프트 + 코드 필터 이중 방어 |
| 생년월일 유출 우려 | 서버 미전송 설계를 UI에 명시 |
| 사주 자체에 거부감 | "MBTI 같은 대화 도구" 프레이밍 유지 |
| 대리 입력으로 제3자 정보가 동의 없이 들어옴 | 팀원별 동의 확인 필수화, 출처 배지 상시 표시, Sprint 3에서 본인 입력으로 전환 |

## 회의록

| 날짜 | 결정 |
|---|---|
| 2026-09-06 | 서비스명 팀사주, 야자시설 채택, v1은 엔진과 UI까지 하고 AI는 Sprint 2로 분리, 스택은 팀 컨벤션 그대로, 진태양시 보정 기본 ON |
| 2026-09-06 | 실제 생시를 동의 받고 입력받는다. v1은 대리 입력 + 팀원별 동의 확인, 보관은 브라우저만. 초대 링크는 Sprint 3 |
| 2026-09-06 | 공유 단위는 팀뿐이다. 개인 사주 카드와 이름은 브라우저 안에만 두고, 공유 뷰에는 팀 유형과 오행 집계만 내보낸다 |
| 2026-09-06 | v1은 저장을 아예 안 한다. localStorage도 안 쓴다. 입력하면 결과만 나오고 새로고침하면 사라진다. 공유는 이미지 추출 하나로만. 링크 공유와 마크다운 복사는 뒤로 미룬다 |
| 2026-09-06 | 디자인은 사주 감성으로. 한지 바탕에 먹 글씨, 주사(다홍) 포인트, 오행은 전통 오방색. 제목은 명조체 |
| 2026-09-06 | 팀 유형마다 いらすとや 무료 일러스트를 붙인다. 총 6장을 public/illust 에 받아둔다 |
| 2026-09-06 | 결과 화면 순서는 요약 타이틀, 일러스트, 분석, 상세 보고서 |
| 2026-09-06 | 분석 전에 로딩 화면을 둔다. 명식을 보는 척하는 문구가 돌아간다 |
| 2026-09-07 | 일러스트는 **결핍 오행**으로 고른다. 유형의 성격은 뭐가 넘치냐가 아니라 뭐가 없냐에서 나온다. 주도 오행으로 고르면 문구는 브레이크가 없다는데 그림만 신나 있다 |
| 2026-09-07 | public/illust 파일은 내용이 바뀌면 이름도 바꾼다. vercel.json 이 1년 immutable 캐시를 걸어둬서, 같은 이름으로 내용만 갈면 이미 받아간 사람은 옛 그림을 계속 본다 |
| 2026-09-07 | 유형 위에 변주를 얹는다. 주도와 결핍 두 변수만으로는 3인 팀과 8인 팀이 같은 결과로 떨어진다. 상생 사슬, 5축 공백, 강약 구성, 인원수 구간을 얹으면 같은 유형도 다르게 읽힌다 |
| 2026-09-07 | 변주에서 사람을 지목하지 않는다. 누가 겉도는지 이름을 쓰면 배제하는 UI 가 된다. "자리" 로만 말하고 처방은 팀이 할 일로 쓴다 |
| 2026-09-07 | 조합 문장은 `src/lib/report/pairs.ts` 가 만든다. 계산은 관계와 방향까지만 낸다. 계산이 카피를 들고 있으면 문구 한 글자 고칠 때마다 계산 테스트가 깨지고, 카피를 손볼 때 saju-writer 가 아니라 saju-engine 영역을 건드려야 한다 |
| 2026-09-07 | 조사는 `src/lib/text/josa.ts` 를 거친다. 이름과 오행을 문장에 끼우는 자리가 계산 레이어에도 있어서 report 밑에 두면 의존 방향이 거꾸로다 |
| 2026-09-07 | 결과 아래에 데려오면 좋은 기운 카드를 넣는다. 팔자 8글자까지 안 가고 오행 하나로 말한다. 실제 팀원을 줄 세우지는 않는다. 여기 나오는 건 아직 팀에 없는 가상의 기운이고, 안 되는 쪽에도 처방이 반드시 붙는다 |
| 2026-09-07 | 랜딩에서 예시 리포트를 먼저 보여준다. 남의 생년월일시까지 받아와야 하는 입력을 시키기 전에 뭐가 나오는지 보여주는 게 맞다. 표본 팀은 예시와 개발 시드가 같은 걸 쓴다 |
| 2026-09-07 | 동의 모달은 입력 화면에서만 띄운다. 동의는 개인정보를 넣을 때 받는 거지 결과를 볼 때 받는 게 아니다 |
| 2026-09-07 | 결과 화면 강조색은 **주도 오행**으로 바꾼다. 넘치는 기운이 그 팀의 성격이라서다. 일러스트는 결핍 기준인데 색은 주도 기준이라 둘이 반대를 가리키는 게 맞다. 하나는 뭐가 넘치는지, 하나는 뭐가 없는지를 말한다 |
| 2026-09-07 | 강조색을 면으로 깔고 글자를 얹는 자리는 글자색도 테마를 따라 뒤집는다. 흰 글자로 고정하면 라이트에서 土 3.04:1, 다크에서 土 2.21:1 로 양쪽 다 깨진다 |
| 2026-09-07 | --accent 는 면과 선, --accent-deep 은 글자로 나눈다. 오방색 원본이 한지색 위에서 土 2.99:1, 金 3.86:1 이라 글자로 쓰면 4.5:1 에 못 미친다 |
| 2026-09-07 | 모션은 CSS 로만 한다. framer-motion 이 설치돼 있지만 쓰는 곳이 없었고, 막대와 레이더는 transition 과 transform 으로 충분하다. 번들을 늘릴 이유가 없다 |
| 2026-09-07 | 화면에 들어올 때 재생하되 폴백 타이머를 같이 둔다. 관찰자나 rAF 가 안 돌면 차트가 0 인 채로 영원히 남는다. 애니메이션을 못 보는 건 참을 수 있어도 결과가 안 보이는 건 안 된다 |
| 2026-09-07 | 삭제에 되돌리기를 붙인다. v1은 아무것도 저장하지 않아서 잘못 지우면 그 사람 생년월일시를 다시 물어봐야 하고, 대리 입력이면 팀원한테 또 물어보는 상황이 된다. 저장을 안 하는 설계일수록 실수 취소가 더 중요하다 |
| 2026-09-07 | 1명만 넣어도 결과를 막지 않는다. 혼자 넣어보면 뭐가 비었는지, 어떤 사람을 붙이면 되는지 알 수 있어서 그게 쓸모다. 대신 화면이 팀인 척하면 안 된다. 조합 집계는 빼고 누굴 데려오면 좋은지를 맨 앞으로 올린다 |
| 2026-09-07 | 팀 5축은 숫자만 두지 않는다. 제일 두꺼운 축과 얇은 축을 짚고, 얇은 쪽에는 오늘 해볼 수 있는 처방을 붙인다. 숫자만 보여주면 읽는 사람이 뭘 해야 할지 모른다 |
| 2026-09-07 | 공유는 파일 공유 시트를 먼저 시도하고 안 되면 내려받기로 떨어진다. iOS Safari 는 download 속성을 무시하고 blob 을 새 탭으로 열고, Firefox 는 앵커가 문서에 붙어 있어야 클릭이 먹는다. 공유가 유일한 유입 경로라 이 하나가 깨지면 유입이 0이 된다 |
| 2026-09-07 | 성별은 v1에서 안 받는다. 대운 방향에 쓰려던 건데 대운 자체가 백로그라 계산에도 화면에도 소비처가 없었다. 쓰지도 않을 개인정보를 대리 입력으로 남의 것까지 받는 건 06-privacy.md 노선과 안 맞는다. 대운을 붙이는 스프린트에 다시 받는다 |
| 2026-09-07 | 관계와 방향을 한 필드씩 두지 않고 판별 유니온으로 묶는다. `relation: tension` 에 `flow: null` 인 값이 타입상 만들어지면 읽는 쪽이 그걸 조용히 a 가 주체인 문장으로 떨어뜨린다. 계산과 해석을 두 파일로 떼어놓으면 이런 보장이 타입에만 남는다 |
| 2026-09-07 | 관계 목록은 유니온에서 파생시킨다. `Exclude<PairRelation, 'same'>` 로 쓰면 새 관계가 자동으로 방향 있는 쪽에 편입되고, `pairCopy` 의 마지막 `return` 이 가드 없는 fallthrough 라 틀린 카피가 조용히 나간다. 관계를 추가할 때 방향 유무를 반드시 정하게 만든다 |
| 2026-09-07 | 타입 확인은 `npm run typecheck` 로 한다. 루트 `tsconfig.json` 이 `"files": []` 에 references 만 있어서 `tsc --noEmit` 은 파일을 하나도 안 보고 항상 통과한다. 올바른 명령에 이름이 없으면 각자 틀린 명령을 발명한다 |
| 2026-09-07 | 공유 카드 푸터의 주소는 커스텀 도메인이 정해질 때까지 안 넣는다. 지금 올리면 `*.vercel.app` 이 나오고 도메인을 붙이면 주소가 두 번 바뀐다. 카드는 이미지라 한 번 퍼지면 못 고친다. 죽은 주소가 박힌 카드는 없느니만 못하다 |
| 2026-09-07 | 동의는 랜딩의 체크박스 한 줄로 받는다. 입력 화면을 전면 모달로 가로막으면 화면이 한 겹 더 쌓이고, 읽지도 않을 문서를 먼저 들이밀게 된다. 받는 시점은 그대로 생년월일을 넣기 전이다 |
| 2026-09-07 | 동의 없이 입력 화면에 서는 상태를 `entryView` 한 곳에서 막는다. 진입점마다 검사하면 새는 데가 생긴다. 실제로 예시 리포트의 "내 팀으로 해보기" 가 그 구멍이었다 |
| 2026-09-07 | 모달은 `dialog` + `showModal()` 로 연다. 포커스 가두기와 Esc 가 공짜다. 다만 열림 상태를 React state 로 거울처럼 들면 Esc 로 닫았을 때 어긋나서 영영 안 열린다. `close` 이벤트는 버블링을 안 해서 `onClose` 로도 못 잡는다. 거울을 두지 말고 `dialog` 를 직접 부린다 |
| 2026-09-07 | 랜딩 시작 버튼은 바닥에 고정한다. 흐름에 두면 유형 목록과 설명에 밀려 접힌 자리 밑으로 내려간다. `bottom-12` 로 띄우면 하단 고지와의 48px 틈으로 본문이 지나가니 `bottom-0` 에 붙이고 패딩으로 고지 높이를 먹는다 |
| 2026-09-07 | 본인 정보인지 대리 입력인지는 라디오 대신 카드로 고른다. 개인정보를 다루는 근거가 갈리는 자리인데 라디오 점은 작아서 흘려보고 지나간다 |
| 2026-09-08 | 화면 문구를 `src/lib/copy/` 로 모은다. 리포트 본문은 `lib/report/` 에 데이터로 모여 있는데 UI 크롬만 컴포넌트 스물몇 개에 흩어져 있었다. 문구 한 벌을 손볼 때 파일을 다 열어야 하고, `ethics-reviewer` 가 감사할 목록이 아예 없다. 경계는 이 문장을 고칠 때 계산이나 명세를 같이 고쳐야 하느냐로 가른다. 명리 용어는 카피가 아니라 도메인 어휘라 `lib/saju/` 와 컴포넌트에 남는다 |
| 2026-09-08 | i18n 라이브러리는 넣지 않는다. 다국어가 백로그에 있긴 하지만 지금 언어는 하나인데 프로바이더와 키 조회, 번역 파일 로딩이 따라붙는다. 실제로 필요한 건 `as const` 로 키를 잠그는 것과 값이 끼는 문장을 함수로 두는 것 둘뿐이고 라이브러리 없이 된다. 영어가 필요해지면 `ko.ts` / `en.ts` 를 파일 단위로 갈아끼운다 |
| 2026-09-08 | 같은 문장이 두 곳에 있어도 상수를 합치지 않는다. 화면과 공유 카드는 한쪽만 문구가 바뀌는 자리가 있어서, 합쳐두면 다음 교체에서 도로 쪼개게 된다. 브랜드명 `팀사주` 만 예외로 `copy/common.ts` 에 하나 둔다. 이건 앞으로도 같이 바뀔 값이다 |
